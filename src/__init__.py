"""Market risk detection package.

This file marks the `src` directory as a package so that its modules
can be imported from notebooks and other scripts without modifying
`sys.path`. It does not need to contain any code.
"""